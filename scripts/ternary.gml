///ternary(condition,true,false)
//The ternary operator from C, Java, ect...
//Returns one value if the condition is true and another if it's false.
//For example, a the abs() function could be created with scrJalastaTernary(x<0,-x,x)
if (argument0)
{
    return argument1;
}
return argument2;
