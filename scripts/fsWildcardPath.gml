/// fsWildcardPath(wildcard, n)
// Returns nth file matching the given wildcard. (e.g. "./*.txt")
// Ensure the path wildcard does not exceed the operating system's MAX_PATH

return external_call(global._fsListPath, argument0, argument1);
