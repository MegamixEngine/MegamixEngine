/// showDebugMessage
/*
identical to the previous show_debug_message, except
I can actually disable it and not have to worry about issues applying
a show_debug_message as a print instead!

Also doesn't 
*/
if (DEBUG_ENABLED && false)
{
    show_debug_message(argument[0]);
}
