/// fsReadLine(fileIndex)
// Reads one line from the given file. Subsequent calls will return the next line.
// Check fsReadLineAvailable(fileIndex) before calling this.

return external_call(global._fsReadLine, argument0);
